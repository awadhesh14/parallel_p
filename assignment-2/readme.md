At turing-gpu
  In parallel_p
    git fetch -all
    git reset --hard origin/master
    git pull

At local
  In Assignment-2
    git add *
    git commit -m "hello mom"
    git push

To Run
  In Sequential-SSSP
    g++ -w sssp.c
    ./a.out > output
